@model IEnumerable<TrainReserveSystem.Models.Booking>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Train_Detail.Source)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total_Fare)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Train_Detail.Source)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Total_Fare)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Booking_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Booking_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Booking_ID })
        </td>
    </tr>
}

</table>
